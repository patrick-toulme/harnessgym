# Numerical Debugging Task

Fix `kernel_ops.py` so `normalized_dot(a, b)` returns the cosine similarity between two equal-length vectors.

Requirements:
- Preserve the public function name and signature.
- Handle randomized floating-point inputs accurately.
- Run `python verifier.py` from this workspace and only report the task solved if it passes.
- Keep the implementation simple and dependency-free.
